// "Guerra" tab: the alliance war territory map (all alliances and their nodes), season info and the wars list.
import React from "react";

import WarScreen from "../alliance/war";

export default function WarTab() {
  return <WarScreen inTab />;
}
